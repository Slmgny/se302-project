package com.uskudar_uni.se302.model;

public class enums {
    public enum ProjectType { COURSE, TUBITAK, TEKNOFEST }

    public enum AuthorityType { OWNER, MEMBER }

    public enum RequestType { JOIN_REQUEST, INVITATION }

    public enum RequestStatus { PENDING, ACCEPTED, REJECTED }
}
